# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log
npm-debug.log*

# Editor / OS junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local environment files
.env
.env.local
public/version.json
render-out/
render-out-*/
quiz/
calib/
Clone/

# Modeller render scratch (agent worktrees / routine output)
scratch-render/
render-out/
hg-out/

# One-off re-export scratch scripts (use ONLY= filter on export-models.mjs instead)
_exp.tmp.*

# The Captain's own working assets — the demo recording and screen grabs.
# Kept out of the repo so the roadmap sweep, which refuses to run on a dirty
# tree, is not blocked by files that are not source.
Chronos Earth.gif
*.png
!public/**/*.png
!dist/**/*.png

# Build-time downloads (Natural Earth etc.) — used to build the maps, never shipped.
.cache/
